
export const defaultOpts = {
    toolBar: true,
    scaleCtrl: true,
    overviewCtrl: true,
    enableScrollWheelZoom: true,
    showIndoorMap: false,
    zoom: 10
};

export const defaultOfflineOpts = {
    retryInterval: 30000,
    txt: 'OFFLINE'
};
